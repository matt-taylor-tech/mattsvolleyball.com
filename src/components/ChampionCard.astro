---
interface Props {
  teamName: string;
  season: string;
  year: number;
  league: string;
  division?: string;
  day: string;
  photo?: string;
}

const { teamName, season, year, league, division, day, photo } = Astro.props;
const r2Base = import.meta.env.PUBLIC_R2_BASE_URL || '';
---

<div class="card group">
  <!-- Photo -->
  {photo ? (
    <div class="aspect-video overflow-hidden">
      <img
        src={`${r2Base}${photo}`}
        alt={`${teamName} - ${season} ${year} Champions`}
        class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
        loading="lazy"
      />
    </div>
  ) : (
    <div class="aspect-video img-placeholder">
      <div class="text-center p-4">
        <svg class="w-12 h-12 mx-auto mb-2 opacity-40" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
        </svg>
        <span class="text-sm font-medium">Team Photo</span>
      </div>
    </div>
  )}

  <!-- Info -->
  <div class="p-5">
    <div class="flex items-center gap-2 mb-2">
      <span class="text-xs font-bold uppercase tracking-wider text-coral-500 bg-coral-500/10 px-2 py-1 rounded-lg">
        🏆 Champions
      </span>
    </div>
    <h3 class="font-display font-bold text-lg text-ocean-600 mb-1">{teamName}</h3>
    <p class="text-sm text-ocean-600/60">
      {season} {year} &middot; {day} {league}
      {division && <span> &middot; {division}</span>}
    </p>
  </div>
</div>
